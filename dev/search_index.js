var documenterSearchIndex = {"docs":
[{"category":"section","location":"interfaces/#Interfaces","page":"Interfaces","text":"This page records the currently supported extension points in GeoVI.jl. The package is still intentionally small, so these interfaces are explicit rather than abstracted behind a large trait system.","title":"Interfaces"},{"category":"section","location":"interfaces/#Likelihoods","page":"Interfaces","text":"All likelihoods subtype AbstractLikelihood and live in the observation space where the Fisher geometry is naturally defined.\n\nRequired methods for a new likelihood MyLikelihood <: AbstractLikelihood:\n\nlogdensity(lh::MyLikelihood, y)\nnormalized_residual(lh::MyLikelihood, y)\ntransformation(lh::MyLikelihood, y)\nleftsqrtmetric(lh::MyLikelihood, y, η)\nrightsqrtmetric(lh::MyLikelihood, y, v)\n\nOptional methods:\n\nfishermetric(lh::MyLikelihood, y, v)\n\nIf fishermetric is omitted, GeoVI uses the default composition leftsqrtmetric(lh, y, rightsqrtmetric(lh, y, v)).\n\nNotes:\n\nlogdensity is interpreted as an unnormalized log density or log likelihood, up to additive constants independent of y.\nenergy(lh, y) is retained as a compatibility alias for -logdensity(lh, y).\nmetric(lh, y, v) is retained as a compatibility alias for fishermetric(lh, y, v).","title":"Likelihoods"},{"category":"section","location":"interfaces/#Variational-Families","page":"Interfaces","text":"Variational families are lightweight markers:\n\nabstract type AbstractVariationalFamily end\n\nTo add a new family, subtype AbstractVariationalFamily and implement\n\n_draw_sample_block(::YourFamily, optimizer, lh, position, rng, config)\n\nreturning a sample block and auxiliary sampler info.","title":"Variational Families"},{"category":"section","location":"interfaces/#Divergences","page":"Interfaces","text":"The current divergence surface is:\n\nabstract type AbstractDivergence end\nconst AbstractFDivergence = AbstractDivergence\n\nAbstractFDivergence is the preferred public alias going forward, while AbstractDivergence remains available for compatibility.\n\nTo add a new divergence, subtype AbstractDivergence and implement:\n\n_kl_value(::YourDivergence, lh, position, residuals)\n_kl_metric(::YourDivergence, lh, position, residuals, v)\n\nThe first method provides the scalar objective minimized by fit, and the second provides the associated Fisher-metric action used by second-order optimizers.","title":"Divergences"},{"category":"section","location":"interfaces/#Optimizers","page":"Interfaces","text":"GeoVI accepts two optimizer families:\n\nbuilt-in optimizers that subtype AbstractOptimizer\nany Optimisers.AbstractRule\n\nTo add a new built-in optimizer, subtype AbstractOptimizer and implement:\n\n_optimize(\n    optimizer::YourOptimizer,\n    x0::AbstractArray;\n    fun_and_grad,\n    hessp,\n    maxiter,\n    miniter,\n    xtol,\n    absdelta,\n    cg_rtol,\n    cg_atol,\n    cg_maxiter,\n    cg_miniter,\n    stepnorm,\n    optimizer_state,\n)\n\nThe return value should match the tuple-like optimization result currently used by NewtonCG and the Optimisers.jl adapter.\n\nIf the backend carries state across outer VI iterations, also implement:\n\n_optimizer_state(optimizer::YourOptimizer, x0, previous_result)\n\nFor simple stateless optimizers, the default _optimizer_state method is enough.","title":"Optimizers"},{"category":"section","location":"interfaces/#AD-Backends","page":"Interfaces","text":"Automatic differentiation backends are selected with ADTypes.jl.\n\nTo add a new backend, implement:\n\n_automatic_linearize(adtype::YourADType, forward, x; fd_eps=1e-6)\n_value_and_gradient(adtype::YourADType, objective, x; fd_eps=1e-6)\n\n_automatic_linearize should return a named tuple with fields:\n\n(value=value_at_x, pushforward=jvp, pullback=vjp)\n\nwhere:\n\npushforward(v) computes J(x) * v\npullback(η) computes J(x)' * η\n\nBackends that need array-specific selection can additionally implement:\n\n_infer_adtype(adtype, x)\n_infer_composed_adtype(adtype, x)\n\nThat pattern is used by the Enzyme and Reactant extensions to pick a backend that matches the storage type of x.","title":"AD Backends"},{"category":"section","location":"design/#Design-Proposal","page":"Design Proposal","text":"This page captures the first-pass package shape for GeoVI.jl. It is intentionally interface-first: the goal is to agree on names, abstractions, and implementation order before we commit to the full numerical machinery.\n\nThe proposal is based on:\n\nFrank, Leike, Ensslin, \"Geometric variational inference\" (Algorithm 2 and the sampling construction in Sections 3.1-3.2)\nnifty.re, especially likelihood.py, evi.py, and optimize_kl.py","title":"Design Proposal"},{"category":"section","location":"design/#Design-Goals","page":"Design Proposal","text":"Stay close to the paper's white-latent formulation, where the prior is standard normal and the posterior geometry is carried by the likelihood.\nPreserve the useful separation already present in nifty.re: likelihood, forward model composition, residual sampling, and outer VI loop.\nBe Julia-first in surface API: callables instead of Python classes where possible, immutable config/state objects, and keyword-driven construction.\nKeep Reactant and Enzyme as execution details of the numerics, not as noise in the user-facing API.\nMake MGVI and geoVI live in the same framework, with geoVI reducing to MGVI when the nonlinear residual update is disabled.","title":"Design Goals"},{"category":"section","location":"design/#Scope-Assumption-For-The-MVP","page":"Design Proposal","text":"The first implementation pass should assume the latent variable xi lives in standard normal coordinates:\n\nxi sim mathcalN(0 I)\n\nThis matches the paper and the structure used in nifty.re. In practice, the user expresses hierarchical priors by providing a forward or generative map from xi into model or data space rather than by supplying an arbitrary prior object.\n\nThat gives us a clean first target:\n\nposterior objective: -logdensity(likelihood, forward(xi)) + 0.5 * dot(xi, xi)\nposterior Fisher metric: I + Fisher pullback through forward\nMGVI: linear residual samples from the inverse Fisher metric\ngeoVI: nonlinear residual update via the metric-induced transformation","title":"Scope Assumption For The MVP"},{"category":"section","location":"design/#Proposed-Package-Layers","page":"Design Proposal","text":"","title":"Proposed Package Layers"},{"category":"section","location":"design/#1.-Dense-array-utilities","page":"Design Proposal","text":"Internal dense-array arithmetic utilities:\n\nrandn_like\nnorm\n\nFor the current scope, the latent state is a single dense array in white coordinates. We do not attempt to support tuples, named tuples, or other structured latent containers in the MVP.","title":"1. Dense-array utilities"},{"category":"section","location":"design/#2.-Likelihood-interface","page":"Design Proposal","text":"The core object should represent a likelihood in the space where the Fisher Fisher geometry is naturally defined. A forward model can then be composed onto it.\n\nProposed minimal interface:\n\nabstract type AbstractLikelihood end\n\nlogdensity(lh::AbstractLikelihood, y)\nnormalized_residual(lh::AbstractLikelihood, y)\ntransformation(lh::AbstractLikelihood, y)\n\nleftsqrtmetric(lh::AbstractLikelihood, y, eta)\nrightsqrtmetric(lh::AbstractLikelihood, y, v)\nfishermetric(lh::AbstractLikelihood, y, v)\n\ncompose(lh::AbstractLikelihood, forward)\n\nIntended semantics:\n\nlogdensity(lh, y) is the unnormalized log likelihood at a prediction y\ntransformation(lh, y) is the local coordinate map whose Jacobian induces the Fisher metric\nleftsqrtmetric and rightsqrtmetric are the square-root actions used by sampling\nfishermetric defaults to leftsqrtmetric(rightsqrtmetric(...))\ncompose(lh, forward) builds a latent-space likelihood by pullback through the forward model\ncompose is best thought of as using a single local linearization of the forward model that provides both pushforward and pullback\nif manual linearization data are not provided, compose can synthesize those actions automatically from an adtype\n\nThe first concrete likelihoods should be:\n\nGaussianLikelihood(data; precision, sqrt_precision=nothing)\nPoissonLikelihood(data; weight=1)\nBernoulliLikelihood(data; weight=1)\nBinomialLikelihood(data; trials, weight=1)\n\nThis is the easiest way to validate the whole stack against Wiener filtering style examples, then move into common canonical exponential-family cases before adding heavier-tailed or variable-covariance models.","title":"2. Likelihood interface"},{"category":"section","location":"design/#3.-Forward-model-composition","page":"Design Proposal","text":"NIFTy's Likelihood.amend(...) is useful, but in Julia I would prefer a more explicit name:\n\nlh_latent = compose(lh_data, forward)\n\nor, if we want a constructor:\n\nlh_latent = ComposedLikelihood(lh_data, forward)\n\nThe forward object itself should just be any callable. If later we want shape metadata or an initializer, we can add a light wrapper, but I would not make Model a required abstraction in v0.\n\nFor manual control, compose can accept either a bundled linearize(x) method returning value, pushforward, and pullback, or explicit pushforward/pullback callables. The preferred default is that compose works with just forward plus an adtype, and only falls back to manual linearization data when needed for performance or unsupported code paths.","title":"3. Forward-model composition"},{"category":"section","location":"design/#Public-Data-Containers","page":"Design Proposal","text":"","title":"Public Data Containers"},{"category":"section","location":"design/#Samples","page":"Design Proposal","text":"We should keep the same useful convention as nifty.re: store residuals relative to an expansion point and materialize posterior samples only when requested.\n\nstruct Samples{P,S,K}\n    position::P\n    residuals::S\n    keys::K\nend\n\nposterior_samples(samples::Samples)\nBase.length(samples::Samples)\nBase.getindex(samples::Samples, i::Int)\nBase.iterate(samples::Samples, state...)\nrecenter(samples::Samples, new_position)\n\nNotes:\n\nposition is the expansion point / latent mean\nresiduals are stored relative to position\nkeys holds RNG seeds or sampler state, when relevant\nposterior_samples(samples) should return position .+ residuals\nboth position and residuals are dense arrays in the current design","title":"Samples"},{"category":"section","location":"design/#VI-configuration-and-state","page":"Design Proposal","text":"Instead of a large hidden optimizer backend, I would make the algorithm mostly functional and carry any optimizer state explicitly.\n\nabstract type AbstractVariationalFamily end\nstruct MGVIFamily <: AbstractVariationalFamily end\nstruct GeoVIFamily <: AbstractVariationalFamily end\n\nabstract type AbstractDivergence end\nconst AbstractFDivergence = AbstractDivergence\nstruct ReverseKL <: AbstractDivergence end\nstruct ForwardKL <: AbstractDivergence end\n\nabstract type AbstractOptimizer end\nstruct NewtonCG <: AbstractOptimizer end\n\nstruct VIConfig{AD,DL,NU,OO}\n    adtype::AD\n    n_iterations::Int\n    n_samples::Int\n    mirrored::Bool\n    draw_linear::DL\n    nonlinear_update::NU\n    optimizer_options::OO\nend\n\nstruct VIState{R,S,M}\n    iteration::Int\n    rng::R\n    sample_state::S\n    minimization_state::M\nend\n\nHere optimizer can be either our built-in NewtonCG() or a standard Optimisers.jl rule such as Optimisers.Adam().\n\nImportant proposed departure from nifty.re:\n\nn_samples should mean the final number of stored samples, not the number of random seeds before mirroring.\nif mirrored=true, require iseven(n_samples) and internally use n_samples ÷ 2 seeds\n\nThat removes a very easy source of confusion from the Python implementation.","title":"VI configuration and state"},{"category":"section","location":"design/#Public-Algorithm-Entry-Points","page":"Design Proposal","text":"","title":"Public Algorithm Entry Points"},{"category":"section","location":"design/#Low-level-sampling-kernels","page":"Design Proposal","text":"These mirror the paper and nifty.re, and give us a clean way to test the pieces independently.\n\ndraw_linear_residual(lh, xi, rng; kwargs...)\nupdate_nonlinear_residual(lh, xi, residual, rng; kwargs...)\ndraw_residual(lh, xi, rng; kwargs...)\n\nIntended meaning:\n\ndraw_linear_residual is the MGVI residual draw\nupdate_nonlinear_residual curves an existing residual into a geoVI sample\ndraw_residual is a convenience wrapper that performs both","title":"Low-level sampling kernels"},{"category":"section","location":"design/#Outer-VI-loop","page":"Design Proposal","text":"Proposed high-level driver:\n\ninitialize_vi(rng; config::VIConfig)\nstep_vi(lh, samples, family, divergence, optimizer, state, config)\nfit([rng], lh, xi0, family, divergence, optimizer; config::VIConfig)\n\nAnd for compatibility with the NIFTy mental model, I would also keep:\n\noptimize_vi(args...; kwargs...) = fit(args...; kwargs...)\noptimize_kl(args...; kwargs...) = fit(args...; kwargs...)\n\nThis makes fit the public entry point while still preserving the paper and NIFTy vocabulary as aliases.","title":"Outer VI loop"},{"category":"section","location":"design/#Example-User-Flow","page":"Design Proposal","text":"This is the kind of surface API I think we should target first:\n\nusing GeoVI\nusing ADTypes\nusing Optimisers\n\nforward(xi) = A * exp.(xi)\n\nlh = GaussianLikelihood(data; precision = inv_noise_cov)\nposterior_lh = compose(lh, forward)\n\ncfg = VIConfig(\n    adtype = ADTypes.AutoEnzyme(),\n    n_iterations = 8,\n    n_samples = 8,\n    mirrored = true,\n    draw_linear = (; cg_maxiter = 100, cg_tol = 1e-4),\n    nonlinear_update = (; maxiter = 5, xtol = 1e-4),\n    optimizer_options = (; maxiter = 35, xtol = 1e-4),\n)\n\nsamples, state = fit(\n    rng,\n    posterior_lh,\n    xi0,\n    GeoVIFamily(),\n    ReverseKL(),\n    Optimisers.Adam(0.05);\n    config = cfg,\n)\ndraws = posterior_samples(samples)\n\nThis should remain valid whether the differentiation engine is finite differences, Enzyme, or later ADTypes.AutoReactant().","title":"Example User Flow"},{"category":"section","location":"design/#ADTypes-Reactant-Positioning","page":"Design Proposal","text":"The package should be designed so that the public API does not care which AD engine is chosen. Instead of a custom backend trait, the user should specify an adtype and we dispatch internally from that.\n\nThat keeps the surface cleaner:\n\nfamily, divergence, and optimizer are explicit algorithm choices\nVIConfig holds tuning knobs like sample counts and tolerances\nadtype selects the differentiation engine via ADTypes\n\nReactant should still not require a separate public backend hierarchy. If the initial point already lives in a Reactant array type, the internal AD dispatch can infer ADTypes.AutoReactant() from that.\n\nInternally we still need implementation hooks for:\n\narray conversion into Reactant buffers when desired\ncompilation / caching of repeated kernels\nAD primitives used for pullbacks, pushforwards, and Fisher-metric actions\nbatching / mapping across multiple sample draws\n\nProposed mental model:\n\nadtype = ADTypes.AutoFiniteDiff() is the reference fallback path\nadtype = ADTypes.AutoEnzyme() is the first real AD path\nadtype = ADTypes.AutoReactant() is the performance path once compilation is wired in\n\nI would avoid exposing raw Enzyme activity wrappers (Const, Duplicated, etc.) in the public API. Those should stay internal helper machinery.","title":"ADTypes + Reactant Positioning"},{"category":"section","location":"design/#Implementation-Order","page":"Design Proposal","text":"","title":"Implementation Order"},{"category":"section","location":"design/#Phase-1:-Foundations","page":"Design Proposal","text":"Dense-array utilities for white latent coordinates\nAbstractLikelihood, GaussianLikelihood, and ComposedLikelihood\nSamples, VIConfig, VIState\na reference finite-difference fallback","title":"Phase 1: Foundations"},{"category":"section","location":"design/#Phase-2:-MGVI-core","page":"Design Proposal","text":"draw_linear_residual\nFisher-metric action and inverse-metric CG solve\nmirrored sampling\nlinear-Gaussian validation against analytic posterior moments","title":"Phase 2: MGVI core"},{"category":"section","location":"design/#Phase-3:-geoVI-core","page":"Design Proposal","text":"transformation-based nonlinear residual objective\nupdate_nonlinear_residual\ndraw_residual\ntoy nonlinear example matching the paper qualitatively","title":"Phase 3: geoVI core"},{"category":"section","location":"design/#Phase-4:-Outer-VI-loop","page":"Design Proposal","text":"step_vi\nfit\nsample reuse / resampling policy\noptimizer and divergence plumbing","title":"Phase 4: Outer VI loop"},{"category":"section","location":"design/#Phase-5:-Reactant-backend","page":"Design Proposal","text":"Reactant buffer conversion and kernel compilation\nEnzyme-based pullback / Fisher-metric actions that survive tracing\nperformance checks against the reference backend","title":"Phase 5: Reactant backend"},{"category":"section","location":"design/#Phase-6:-Feature-parity-extensions","page":"Design Proposal","text":"point estimates and frozen constants\nadditional likelihoods\nstructured diagnostics and minisanity-like summaries\ncheckpoint / resume","title":"Phase 6: Feature parity extensions"},{"category":"section","location":"design/#Current-Modeling-Choice","page":"Design Proposal","text":"The package now assumes:\n\nall latent parameters are represented as a single dense array\nthose latent parameters live in IID standard normal coordinates\nhierarchical structure is handled by the forward / generative map, not by a structured latent container\nthe top-level fitting API is fit, with configurable family, optimizer, and divergence\n\nThis keeps the implementation focused on the white-parameter geometry from the paper and avoids carrying generic tree machinery that the intended use case does not need.","title":"Current Modeling Choice"},{"category":"section","location":"#GeoVI","page":"Home","text":"Documentation for GeoVI.\n\nThe initial review-first package architecture and API proposal lives on the Design Proposal page.\n\nThe current implementation-facing extension hooks for new likelihoods, optimizers, and AD backends are collected on the Interfaces page.\n\n","title":"GeoVI"},{"category":"type","location":"#GeoVI.AbstractDivergence","page":"Home","text":"AbstractDivergence\nAbstractFDivergence\n\nMarker supertype for divergence objectives optimized by the outer VI loop.\n\nTo add a new divergence, subtype AbstractDivergence and implement the internal objective hooks _kl_value(::YourDivergence, ...) and _kl_metric(::YourDivergence, ...).\n\n\n\n\n\n","title":"GeoVI.AbstractDivergence"},{"category":"type","location":"#GeoVI.AbstractLikelihood","page":"Home","text":"AbstractLikelihood\n\nAbstract supertype for likelihood objects defined in the observation space.\n\nNew likelihood implementations should define:\n\nlogdensity(lh, y)\nnormalized_residual(lh, y)\ntransformation(lh, y)\nleftsqrtmetric(lh, y, η)\nrightsqrtmetric(lh, y, v)\n\nfishermetric(lh, y, v) defaults to the composition of the left and right square-root actions, and energy(lh, y) remains available as the compatibility alias -logdensity(lh, y).\n\n\n\n\n\n","title":"GeoVI.AbstractLikelihood"},{"category":"type","location":"#GeoVI.AbstractOptimizer","page":"Home","text":"AbstractOptimizer\n\nMarker supertype for built-in optimizer backends.\n\nTo add a new optimizer backend, subtype AbstractOptimizer and implement _optimize(optimizer, x0; fun_and_grad, hessp, kwargs...). If the backend needs persistent state across VI iterations, also implement _optimizer_state(optimizer, x0, previous_result).\n\nAny Optimisers.AbstractRule is also accepted directly without subtyping AbstractOptimizer.\n\n\n\n\n\n","title":"GeoVI.AbstractOptimizer"},{"category":"type","location":"#GeoVI.AbstractVariationalFamily","page":"Home","text":"AbstractVariationalFamily\n\nMarker supertype for variational-sampling schemes.\n\nTo add a new family, subtype AbstractVariationalFamily and provide a _draw_sample_block(::YourFamily, optimizer, likelihood, position, rng, config) method.\n\n\n\n\n\n","title":"GeoVI.AbstractVariationalFamily"},{"category":"method","location":"#GeoVI.draw_linear_residual-Tuple{AbstractLikelihood, Any, Random.AbstractRNG}","page":"Home","text":"draw_linear_residual(lh, xi, rng; kwargs...)\n\nDraw a linear MGVI residual around expansion point xi.\n\nThe draw is performed in white latent coordinates, so the covariance of the returned residual is the inverse posterior Fisher metric\n\nI + fishermetric(lh, xi, ·).\n\nIf from_inverse=false, the function returns a draw with covariance equal to the posterior Fisher metric itself instead.\n\n\n\n\n\n","title":"GeoVI.draw_linear_residual"},{"category":"function","location":"#GeoVI.logdensity","page":"Home","text":"logdensity(lh, y)\n\nReturn the unnormalized log density induced by lh at prediction y, up to additive constants independent of y.\n\n\n\n\n\n","title":"GeoVI.logdensity"}]
}
